{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Medict Tauri migration window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
